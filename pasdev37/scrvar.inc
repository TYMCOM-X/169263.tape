VAR
    EXIT_FLAG : BOOLEAN;		(* /E SWITCH *)
    RUN_FLAG  : BOOLEAN;		(* /RUN SWITCH *)
    RUNOFF_VAL : INTEGER;		(* /RUNOFFSET VALUE *)
    RUN_PGM_NAME : STRING[75];		(* /RUN ??? PROGRAM NAME *)
    OPT_NAME : STRING[30];		(* FOR SCRIBE SWITCHES *)
